---
import type { StoryblokAlert } from '../_dto/AlertDTO.astro';

interface Props {
    blok: StoryblokAlert;
    className?: string;
}

const { blok, className } = Astro.props;
---

<div class:list={[className, 'bg-yellow-700/10 dark:bg-yellow-400/10, text-yellow-700 dark:text-yellow-400']} data-blok-c={blok.storyblokData?.['data-blok-c']} data-blok-uid={blok.storyblokData?.['data-blok-uid']}>
    <slot /> 
</div>