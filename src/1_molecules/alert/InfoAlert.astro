---
import type { StoryblokAlert } from '../_dto/AlertDTO.astro';

interface Props {
    blok: StoryblokAlert;
    className?: string;
}

const { blok, className } = Astro.props;
---

<div class:list={[className, 'bg-info-700/10 dark:bg-info-400/10, text-info-700 dark:text-info-400']} data-blok-c={blok.storyblokData?.['data-blok-c']} data-blok-uid={blok.storyblokData?.['data-blok-uid']}>
    <slot /> 
</div>